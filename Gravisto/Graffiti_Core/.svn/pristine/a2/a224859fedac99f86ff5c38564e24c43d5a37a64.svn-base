if (activated())
{
    ins["cursor"] = Cursor.HAND_CURSOR;
    action("setCursor");
}
else if (matches("mouse.press", ["button", "LEFT"]))
{
    // var center = out["position"];
    // ins["center"] = center;
    // ins["rawCenter"] = out["rawPosition"];
    // action("startZoomRotation");
    // ins["position"] = center;
    //action("setCompassPosition");
    action("contractIncomingEdge");
}
//else if (matches("mouse.drag", ["button", "LEFT"]))
//{
//    ins["delta"] = out["rawDelta"];
 //   action("pan");
//}
//else if (matches("mouse.drag", ["button", "RIGHT"]))
//{
    //var pos = out["rawPosition"];
    //ins["snap"] = matches("mouse.drag", ["button", "RIGHT"], ["alt", "REQUIRE_UP"]);
    //ins["rawPosition"] = pos;
    //action("zoomRotation");
//}
//else if (matches("mouse.release", ["button", "RIGHT"]))
//{
 //   reset();
  //  ins["cursor"] = Cursor.HAND_CURSOR;
   // action("setCursor");
//}

package quoggles.icons;

import quoggles.boxes.IBox;

/**
 * Interface for icons used as buttons for adding new boxes to the system.
 */
public interface IBoxIcon {
    
    /**
     * Each time this method is called, a new instance of the box class
     * (implementing interface <code>IBox</code>) is returned.
     * 
     * @return new instance of class implementing interface <code>IBox</code>
     */
    public IBox getNewBoxInstance();
}
